{
  "name": "vue-nuxt2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pm2:start": "npx pm2 start npm --name nuxt-ssr -- run start",
    "pm2:stop": "npx pm2 stop nuxt-ssr",
    "pm2:restart": "npx pm2 restart nuxt-ssr",
    "pm2:logs": "npx pm2 logs",
    "pm2:save": "npx pm2 save",
    "pm2:startup": "npx pm2 startup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.6",
    "cookie-universal-nuxt": "^2.1.5",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7",
    "nuxt-i18n": "^6.28.1",
    "vuex-persist": "^2.2.0"
  },
  "devDependencies": {
    "pm2": "^6.0.5"
  }
}
